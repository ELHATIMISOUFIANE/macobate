---
// HeaderResponsive.astro - Barre de navigation responsive avec liens ancrés
const mainMenuItems = [
  {
    title: "Accueil",
    url: "/",
    hasSubmenu: false,
    showInMainOnly: false,
  },
  {
    title: "Qui sommes-nous",
    url: "/about",
    hasSubmenu: true,
    showInMainOnly: false,
    submenu: [
      { title: "Mot de la Direction", url: "/about#company-section" },
      { title: "Nos Points Forts", url: "/about#highlights-section" },
      { title: "Notre Histoire", url: "/about#history-section" },
      { title: "Nos Réalisations", url: "/about#achived-section" },
      { title: "Notre Équipe", url: "/about#team-section" },
    ],
  },
  {
    title: "Activités",
    url: "/market",
    hasSubmenu: true,
    showInMainOnly: false,
    submenu: [
      {
        title: "Construction Métallique",
        url: "/market-single?sector=construction",
      },
      {
        title: "Chaudronnerie Industrielle",
        url: "/market-single?sector=chaudronnerie",
      },
      {
        title: "Équipements Hydromécaniques",
        url: "/market-single?sector=hydraulique",
      },
      { title: "Traitement des Eaux", url: "/market-single?sector=eau" },
      {
        title: "Maintenance Industrielle",
        url: "/market-single?sector=maintenance",
      },
    ],
  },
  {
    title: "Produits",
    url: "/products",
    hasSubmenu: true,
    showInMainOnly: false,
    submenu: [
      {
        title: "Eau",
        url: "/products/eau",
        hasSubmenu: true,
        childSubmenu: [
          { title: "Vannes Segment", url: "/products/eau/vannes-segment" },
          { title: "Vannes Wagon", url: "/products/eau/vannes-wagon" },
          { title: "Batardeaux", url: "/products/eau/batardeaux" },
          { title: "Grilles", url: "/products/eau/grilles" },
        ],
      },
      {
        title: "Environnement",
        url: "/products/environnement",
        hasSubmenu: true,
        childSubmenu: [
          { title: "Dégrilleurs", url: "/products/environnement/degrilleurs" },
          { title: "Compacteurs", url: "/products/environnement/compacteurs" },
          {
            title: "Pont Racleur",
            url: "/products/environnement/pont-racleur",
          },
        ],
      },
      {
        title: "Industrie",
        url: "/products/industrie",
        hasSubmenu: true,
        childSubmenu: [
          { title: "Réservoirs", url: "/products/industrie/reservoirs" },
          { title: "Silos", url: "/products/industrie/silos" },
          { title: "Charpentes", url: "/products/industrie/charpentes" },
        ],
      },
    ],
  },
  {
    title: "Réalisations",
    url: "/projects",
    hasSubmenu: false,
    showInMainOnly: true,
  },
  {
    title: "Actualités",
    url: "/blog",
    hasSubmenu: false,
    showInMainOnly: true,
  },
  {
    title: "Contact",
    url: "/contact",
    hasSubmenu: false,
    showInMainOnly: true,
  },
];

// Filtrer les éléments pour le menu principal (exclure les éléments marqués showInMainOnly = true)
const mainMenuFiltered = mainMenuItems.filter((item) => !item.showInMainOnly);
---

<!-- En-tête Principal Responsive-->
<header class="main-header">
  <!-- Barre Supérieure -->
  <div class="header-top">
    <div class="auto-container">
      <div class="top-outer clearfix">
        <!--Section Gauche-->
        <div class="top-left">
          <ul class="links clearfix">
            <li>
              <a href="tel:+212537659646"
                ><span class="icon fa fa-phone"></span><span class="contact-text">+212 5 37 65 96 46</span></a
              >
            </li>
            <li>
              <a href="mailto:contact@macobate.com"
                ><span class="icon fa fa-envelope-o"
                ></span><span class="contact-text">contact@macobate.com</span></a
              >
            </li>
            <li class="hide-on-mobile">
              <a href="https://www.google.com/maps?q=34.021972,-6.836359"
                ><span class="icon flaticon-placeholder-2"></span><span class="contact-text">Zone Industrielle Takadoum, Rabat</span></a
              >
            </li>
          </ul>
        </div>

        <!--Section Droite-->
        <div class="top-right clearfix">
          <ul class="clearfix">
            <!--Langue-->
            <li class="language dropdown">
              <a
                class="btn btn-default dropdown-toggle"
                id="dropdownMenu2"
                data-toggle="dropdown"
                aria-haspopup="true"
                aria-expanded="true"
                href="#"
                ><span class="flag-img"
                  ><img src="/images/icons/flag.jpg" alt="Drapeau" /></span
                ><span class="language-text">Français</span> &nbsp;<span class="fa fa-angle-down"></span></a
              >
              <ul
                class="dropdown-menu style-one"
                aria-labelledby="dropdownMenu2"
              >
                <li><a href="#">Français</a></li>
                <li><a href="#">Anglais</a></li>
                <li><a href="#">Arabe</a></li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <!-- Fin Barre Supérieure -->

  <!-- Boîte Principale -->
  <div class="main-box">
    <div class="auto-container">
      <div class="outer-container clearfix">
        <!--Boîte Logo avec classe ajustée-->
        <div class="logo-box ">
          <div class="logo">
            <a href="/"><img src="/images/logo.png" alt="Logo MACOBATE" /></a>
          </div>
        </div>

        <!--Navigation Externe-->
        <div class="nav-outer clearfix">
          <!-- Menu Principal avec sous-menus améliorés -->
          <nav class="main-menu">
            <div class="navbar-header">
              <!-- Bouton Toggle Mobile -->
              <button
                type="button"
                class="navbar-toggle"
                data-toggle="collapse"
                data-target=".navbar-collapse"
              >
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
            </div>

            <div class="navbar-collapse collapse clearfix">
              <ul class="navigation clearfix">
                {/* Menu principal filtré - seulement 5 éléments */}
                {mainMenuFiltered.map((item) => (
                  <li class={item.hasSubmenu ? "dropdown" : ""}>
                    <a href={item.url}>
                      {item.title}
                      {item.hasSubmenu && <span class="fa fa-angle-down"></span>}
                    </a>
                    {item.hasSubmenu && (
                      <ul>
                        {item.submenu.map((subItem) => (
                          <li class={subItem.hasSubmenu ? "dropdown" : ""}>
                            <a href={subItem.url}>
                              {subItem.title}
                              {subItem.hasSubmenu && <span class="fa fa-angle-right"></span>}
                            </a>
                            {subItem.hasSubmenu && (
                              <ul>
                                {subItem.childSubmenu.map((childItem) => (
                                  <li><a href={childItem.url}>{childItem.title}</a></li>
                                ))}
                              </ul>
                            )}
                          </li>
                        ))}
                      </ul>
                    )}
                  </li>
                ))}
              </ul>
            </div>
          </nav>
          <!-- Fin Menu Principal-->

          <!--Boîte Externe-->
          <div class="outer-box">
            <a
              href="#"
              class="theme-btn quote-btn"
              data-toggle="modal"
              data-target="#schedule-box"
              >Demander un devis<span class="icon"></span></a
            >
            <ul class="social-icon-one hide-on-mobile">
              <li><a href="#"><span class="fa fa-facebook"></span></a></li>
              <li><a href="#"><span class="fa fa-linkedin"></span></a></li>
              <li><a href="#"><span class="fa fa-youtube"></span></a></li>
            </ul>
          </div>
        </div>
        <!--Fin Navigation Externe-->
      </div>
    </div>
  </div>

  <!--En-tête Fixe avec alignement ajusté-->
  <div class="sticky-header">
    <div class="auto-container">
      <div class="outer-container clearfix">
        <!--Boîte Logo avec classe ajustée-->
        <div class="logo-box logo-aligned pull-left">
          <div class="logo">
            <a href="/"><img src="/images/logo-small.png" alt="Logo MACOBATE" /></a>
          </div>
        </div>

        <!--Navigation Externe-->
        <div class="nav-outer clearfix">
          <!-- Menu Principal -->
          <nav class="main-menu">
            <div class="navbar-header">
              <!-- Bouton Toggle Mobile -->
              <button
                type="button"
                class="navbar-toggle"
                data-toggle="collapse"
                data-target=".navbar-collapse"
              >
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
            </div>

            <div class="navbar-collapse collapse clearfix">
              <ul class="navigation clearfix">
                {/* Menu complet - tous les éléments */}
                {mainMenuItems.map((item) => (
                  <li class={item.hasSubmenu ? "dropdown" : ""}>
                    <a href={item.url}>
                      {item.title}
                      {item.hasSubmenu && <span class="fa fa-angle-down"></span>}
                    </a>
                    {item.hasSubmenu && (
                      <ul>
                        {item.submenu.map((subItem) => (
                          <li class={subItem.hasSubmenu ? "dropdown" : ""}>
                            <a href={subItem.url}>
                              {subItem.title}
                              {subItem.hasSubmenu && <span class="fa fa-angle-right"></span>}
                            </a>
                            {subItem.hasSubmenu && (
                              <ul>
                                {subItem.childSubmenu.map((childItem) => (
                                  <li><a href={childItem.url}>{childItem.title}</a></li>
                                ))}
                              </ul>
                            )}
                          </li>
                        ))}
                      </ul>
                    )}
                  </li>
                ))}
              </ul>
            </div>
          </nav>
          <!-- Fin Menu Principal-->
        </div>
        <!--Fin Navigation Externe-->
      </div>
    </div>
  </div>
  <!--Fin En-tête Fixe-->
</header>
<!--Fin En-tête Principal -->

<style>
  /* Styles pour le positionnement du logo */
  .logo-box {
    position: relative;
    padding-right: 30px; /* Ajoute un espace à droite du logo */
    margin-right: 20px; /* Augmente la marge à droite */
  }
  
  .logo-aligned {
    margin-left: -15px; /* Déplace légèrement le logo vers la gauche */
    position: relative;
  }
  
  .logo {
    position: relative;
 
  }
  
  .logo img {
    max-width: 100%;
    height: auto;
  }
  
  /* Ajustement pour l'alignement avec le menu */
  .main-box .outer-container {
    display: flex;
    align-items: center;
    flex-wrap: wrap; /* Important pour responsive */
  }
  
  .main-box .nav-outer {
    flex: 1;
    padding-left: 20px; /* Espace entre le logo et le menu */
  }
  
  /* Ajustement pour le header sticky */
  .sticky-header .logo-box {
    padding-right: 15px;
    margin-right: 10px;
  }
  
  .sticky-header .logo-aligned {
    margin-left: -10px; /* Ajustement pour le logo dans le header sticky */
  }
  
  /* Styles pour les sous-menus améliorés */
  .main-menu .navigation > li > ul {
    position: absolute;
    left: 0px;
    top: 100%;
    width: 230px;
    z-index: 100;
    display: none;
    background: #ffffff;
    border-top: 3px solid #0f3182;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
  }
  
  .main-menu .navigation > li > ul > li {
    position: relative;
    width: 100%;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }
  
  .main-menu .navigation > li > ul > li:last-child {
    border-bottom: none;
  }
  
  .main-menu .navigation > li > ul > li > a {
    position: relative;
    display: block;
    padding: 10px 15px;
    line-height: 24px;
    font-weight: 400;
    font-size: 14px;
    color: #333333;
    text-align: left;
    text-transform: capitalize;
    transition: all 500ms ease;
    -moz-transition: all 500ms ease;
    -webkit-transition: all 500ms ease;
    -ms-transition: all 500ms ease;
    -o-transition: all 500ms ease;
  }
  
  .main-menu .navigation > li > ul > li:hover > a {
    background: #0f3182;
    color: #ffffff;
  }
  
  /* Sous-menus de niveau 3 */
  .main-menu .navigation > li > ul > li > ul {
    position: absolute;
    left: 100%;
    top: 0px;
    width: 230px;
    z-index: 100;
    display: none;
    background: #ffffff;
    border-top: 3px solid #0f3182;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
  }
  
  .main-menu .navigation > li > ul > li > ul > li {
    position: relative;
    width: 100%;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }
  
  .main-menu .navigation > li > ul > li > ul > li:last-child {
    border-bottom: none;
  }
  
  .main-menu .navigation > li > ul > li > ul > li > a {
    position: relative;
    display: block;
    padding: 10px 15px;
    line-height: 24px;
    font-weight: 400;
    font-size: 14px;
    color: #333333;
    text-align: left;
    text-transform: capitalize;
    transition: all 500ms ease;
    -moz-transition: all 500ms ease;
    -webkit-transition: all 500ms ease;
    -ms-transition: all 500ms ease;
    -o-transition: all 500ms ease;
  }
  
  .main-menu .navigation > li > ul > li > ul > li:hover > a {
    background: #0f3182;
    color: #ffffff;
  }
  
  /* Afficher les sous-menus au survol */
  .main-menu .navigation li:hover > ul {
    display: block;
  }
  
  /* Indicateur de menu actif */
  .main-menu .navigation > li.current > a,
  .main-menu .navigation > li:hover > a {
    color: #0f3182;
    opacity: 1;
  }
  
  .main-menu .navigation > li > ul > li.current > a,
  .main-menu .navigation > li > ul > li > ul > li.current > a {
    background: #0f3182;
    color: #ffffff;
  }
  
  /* Ajustement de l'espacement des éléments de menu */
  .main-menu .navigation > li {
    margin-left: 5px;
    margin-right: 5px;
  }
  
  /* Améliorer l'alignement du menu avec le logo */
  @media (min-width: 992px) {
    .main-box .nav-outer {
      display: flex;
      align-items: center;
    }
    
    .main-menu {
      margin-left: -15px; /* Ajuster pour aligner avec le logo */
    }
    
    /* Cacher le bouton mobile sur desktop */
    .navbar-toggle {
      display: none;
    }
  }
  
  /* STYLES RESPONSIVES */
  
  /* Tablettes et petits écrans */
  @media (max-width: 991px) {
    .logo-box {
      padding: 15px 0;
      width: 180px;
    }
    
    .main-box .outer-container {
      flex-direction: column;
      align-items: flex-start;
    }
    
    .main-box .nav-outer {
      width: 100%;
      padding-left: 0;
    }
    
    .main-menu .navbar-collapse {
      max-height: 300px;
      overflow-y: auto;
    }
    
    .main-menu .navigation > li > ul, 
    .main-menu .navigation > li > ul > li > ul {
      position: static;
      width: 100%;
      box-shadow: none;
      display: none;
    }
    
    .main-menu .navigation > li.dropdown > a:after {
      display: none;
    }
    
    .main-menu .navigation {
      padding-top: 15px;
    }
    
    .main-menu .navigation > li {
      margin: 0;
      display: block;
      width: 100%;
    }
    
    .main-menu .navigation > li > a {
      padding: 10px 15px;
      border-bottom: 1px solid rgba(0,0,0,0.1);
    }
    
    /* Transformation des sous-menus en accordéon */
    .main-menu .navigation li.dropdown .dropdown-btn {
      position: absolute;
      right: 10px;
      top: 6px;
      width: 34px;
      height: 30px;
      border: 1px solid #dddddd;
      text-align: center;
      font-size: 16px;
      line-height: 28px;
      background: #ffffff;
      cursor: pointer;
      z-index: 5;
    }
    
    .outer-box {
      position: absolute;
      right: 60px;
      top: 0;
      z-index: 12;
    }
    
    .sticky-header .logo-box {
      width: 160px;
    }
  }
  
  /* Mobiles */
  @media (max-width: 767px) {
    .header-top .top-left .contact-text,
    .header-top .top-right .language-text,
    .hide-on-mobile {
      display: none;
    }
    
    .header-top .top-left {
      width: 100%;
      text-align: center;
    }
    
    .header-top .top-left .links li {
      margin: 0 10px;
      display: inline-block;
      float: none;
    }
    
    .header-top .top-right {
      width: 100%;
      text-align: center;
      margin-top: 5px;
    }
    
    .header-top .language.dropdown {
      display: inline-block;
      float: none;
    }
    
    .sticky-header {
      display: none !important;
    }
    
    .main-box .logo-box {
      width: 150px;
      margin: 0 auto;
      padding: 10px 0;
      float: none;
      text-align: center;
    }
    
    .main-box .outer-box {
      position: static;
      width: 100%;
      margin: 10px 0;
      text-align: center;
    }
    
    .main-box .outer-box .quote-btn {
      display: block;
      margin: 0 auto;
      width: 200px;
    }
    
    .main-box .nav-outer {
      margin-top: 15px;
    }
    
    .navbar-toggle {
      display: block;
      float: none;
      margin: 0 auto;
    }
  }
  
  /* Très petits écrans */
  @media (max-width: 479px) {
    .header-top .top-left .links li {
      margin: 0 5px;
    }
    
    .main-box .logo-box {
      width: 120px;
    }
    
    .main-box .outer-box .quote-btn {
      width: 180px;
      font-size: 12px;
      padding: 8px 15px;
    }
    
    .navbar-toggle {
      padding: 6px 8px;
    }
  }
</style>

<script is:inline>
  // Script pour gérer l'état actif des liens de navigation et le défilement vers les ancres
  document.addEventListener('DOMContentLoaded', function() {
    const currentPath = window.location.pathname;
    const currentHash = window.location.hash;
    const navItems = document.querySelectorAll('.navigation li a');
    
    // Fonction pour faire défiler en douceur vers les ancres
    const smoothScroll = function(targetId) {
      const targetElement = document.getElementById(targetId);
      if (targetElement) {
        const headerOffset = 100; // Hauteur de l'en-tête fixe
        const elementPosition = targetElement.getBoundingClientRect().top;
        const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
        
        window.scrollTo({
          top: offsetPosition,
          behavior: "smooth"
        });
      }
    };
    
    // Ajouter l'ID aux sections pour les ancres
    const sections = {
      "company-section": document.querySelector('.company-section'),
      "highlights-section": document.querySelector('.highlights-section'),
      "history-section": document.querySelector('.history-section'),
      "achived-section": document.querySelector('.achived-section'),
      "team-section": document.querySelector('.team-section')
    };
    
    // Ajouter les IDs aux sections s'ils existent
    for (const [id, section] of Object.entries(sections)) {
      if (section) section.id = id;
    }
    
    // Traitement des liens d'ancrage
    navItems.forEach(item => {
      // Vérifier si le lien a une ancre
      if (item.getAttribute('href').includes('#')) {
        item.addEventListener('click', function(e) {
          const href = this.getAttribute('href');
          
          // Si nous sommes sur la même page que l'ancre
          if (href.startsWith('#') || (currentPath === href.split('#')[0])) {
            e.preventDefault();
            const targetId = href.split('#')[1];
            smoothScroll(targetId);
            
            // Mettre à jour l'URL sans rechargement
            history.pushState(null, null, href);
          }
        });
      }
      
      // Vérifier si le lien correspond au chemin actuel
      if (item.getAttribute('href') === currentPath || 
          (currentHash && item.getAttribute('href') === currentPath + currentHash)) {
        item.parentElement.classList.add('current');
        
        // Si l'élément est dans un sous-menu, marquer également le parent
        const parentLi = item.parentElement.parentElement.parentElement;
        if (parentLi && parentLi.classList.contains('dropdown')) {
          parentLi.classList.add('current');
        }
      }
      
      // Vérifier les paramètres d'URL pour les pages de secteur
      if (currentPath.includes('market-single') || currentPath.includes('products')) {
        const urlParams = new URLSearchParams(window.location.search);
        const sector = urlParams.get('sector');
        
        if (sector && item.getAttribute('href').includes(sector)) {
          item.parentElement.classList.add('current');
          const parentLi = item.parentElement.parentElement.parentElement;
          if (parentLi && parentLi.classList.contains('dropdown')) {
            parentLi.classList.add('current');
          }
        }
      }
    });
    
    // Si nous arrivons directement sur une page avec une ancre, faire défiler vers cette ancre
    if (currentHash) {
      setTimeout(function() {
        smoothScroll(currentHash.substring(1));
      }, 500);
    }
    
    // Script pour transformer les sous-menus en accordéon sur mobile
    if (window.innerWidth <= 991) {
      // Ajouter des boutons +/- pour les sous-menus sur mobile
      document.querySelectorAll('.navigation li.dropdown').forEach(function(item) {
        const dropdownBtn = document.createElement('div');
        dropdownBtn.className = 'dropdown-btn';
        dropdownBtn.innerHTML = '+';
        item.appendChild(dropdownBtn);
        
        dropdownBtn.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();
          
          const subMenu = this.parentElement.querySelector('ul');
          if (subMenu) {
            if (subMenu.style.display === 'block') {
              subMenu.style.display = 'none';
              this.innerHTML = '+';
            } else {
              subMenu.style.display = 'block';
              this.innerHTML = '-';
            }
          }
        });
      });
    }
  });
  
  // Gestion du header sticky
  // Ajouter cette fonction pour une animation plus professionnelle du menu sticky
window.addEventListener('scroll', function() {
  const stickyHeader = document.querySelector('.sticky-header');
  const mainHeader = document.querySelector('.main-header');
  
  if (window.innerWidth > 767) {
    if (window.pageYOffset > 150) { // Augmenter légèrement le seuil
      stickyHeader.classList.add('is-sticky');
      stickyHeader.style.display = 'block';
      stickyHeader.style.position = 'fixed';
      stickyHeader.style.top = '0';
      stickyHeader.style.left = '0';
      stickyHeader.style.right = '0';
      stickyHeader.style.zIndex = '999';
      stickyHeader.style.animation = 'fadeIn 0.4s ease-out forwards';
      
      // Ajouter une petite classe au header principal
      mainHeader.classList.add('has-sticky-active');
    } else {
      stickyHeader.classList.remove('is-sticky');
      stickyHeader.style.display = 'none';
      mainHeader.classList.remove('has-sticky-active');
    }
  }
});

// Amélioration de l'interaction des sous-menus sur desktop
if (window.innerWidth > 991) {
  document.querySelectorAll('.navigation li.dropdown').forEach(function(item) {
    item.addEventListener('mouseenter', function() {
      const subMenu = this.querySelector('ul');
      if (subMenu) {
        subMenu.style.display = 'block';
        subMenu.style.opacity = '0';
        subMenu.style.transform = 'translateY(10px)';
        
        // Forcer un reflow
        subMenu.offsetHeight;
        
        // Animer l'apparition
        subMenu.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
        subMenu.style.opacity = '1';
        subMenu.style.transform = 'translateY(0)';
      }
    });
    
    item.addEventListener('mouseleave', function() {
      const subMenu = this.querySelector('ul');
      if (subMenu) {
        subMenu.style.opacity = '0';
        subMenu.style.transform = 'translateY(10px)';
        
        setTimeout(function() {
          if (!item.matches(':hover')) {
            subMenu.style.display = 'none';
          }
        }, 300);
      }
    });
  });
}
</script>